fastapi==0.115.8
uvicorn[standard]==0.30.6
# Shared CHEEAPPS venv (Python 3.12, e.g. X:\.env): voice-dictation uses >=0.5.1; do not pin 0.4.6 (downgrade locks portaudio DLL on Windows)
sounddevice>=0.5.1,<0.6
soundfile==0.12.1
numpy==1.26.4
# pyannote 3.3.x imports torchaudio.AudioMetaData (removed in torchaudio 2.9+). Pin a matched stack.
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1
faster-whisper==1.1.0
whisperx==3.3.1
requests==2.32.4
pyannote.audio==3.3.2
python-multipart==0.0.9
aiohttp==3.9.3
diart==0.9.0
httpx==0.28.1
# 0.7 was never published to PyPI; OpusBufferedEncoder is in 0.6.14a1 (optional — app falls back to WAV)
PyOgg>=0.6.14a1